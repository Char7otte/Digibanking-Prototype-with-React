<%
var bodyContent = `
<div class="card">
  <h2>Your Profile</h2>

  <p><span class="label">Name:</span> \\<%= user?.name %></p>
  <p><span class="label">Email:</span> \\<%= user?.email %></p>
  <p><span class="label">Account No:</span> \\<%= user?.accountNumber %></p>

  <p class="auth-meta">
      <a href="/dashboard" class="link-primary">Back to dashboard</a>
  </p>
</div>
`;
%>

<%- include('layout', { title: "Profile - OCBC Digital", body: bodyContent }) %>
